# ТЕХНОМАГИЯ — заказ на всё оставшееся

Один документ, самодостаточный: его пересылают целиком. Здесь **весь
остаток работы по графике** — 103 файла. Больше просить будет нечего.

Игра — браузерный экшен про техномагов-раздолбаев в постапокалиптическом
парке. Стихии смешиваются, вещество остаётся лежать на полу, разряд идёт по
разлитой воде, а найденные заклинания записываются в книгу.

---

# Что уже принято и стоит в игре

Не переделывать, а **смотреть как на образец**:

* **16 значков заклинаний** — приняты и уже работают в книге. Средняя
  схожесть внутри набора 33 % при планке 45 %, ни одной близкой пары. Это
  нужный уровень.
* **9 портретов** — настоящие лица во весь кадр, у каждого своя причёска.
* **5 значков стихий** — белые силуэты, игра красит их сама.
* Плитки пола, шесть листов стен, интерфейс, посохи и вещицы.

---

# Три проверки перед отправкой

Делаются за минуту и экономят круг переписки.

**1. Схожесть.** Уменьшить каждый файл до 32×32, взять непрозрачные точки,
для каждой пары внутри набора поделить общую площадь на суммарную.
**Средняя ниже 45 %, ни одной пары выше 80 %.** Меняется цвет — число не
меняется; меняется рисунок — меняется. Прошлый заход дал 59 % у значков и
75 % у предметов при 173 почти совпавших парах из 465 — так нельзя.

**2. Серым.** Убрать цвет. Набор обязан остаться различимым.

**3. Сорок пикселей.** Фигуры уменьшить до 40 точек, положить на фон
`#16342d`: различаются ли, видно ли, куда смотрят. На экране маг занимает
26–40 пикселей, и всё тоньше пропадает.

---

# 1. Значки, 26 файлов, 64×64

Значок — знак вещи или явления: **одна крупная форма**, узнаваемая без
цвета, читаемая в 24 пикселя. Прозрачный фон.

## Оставшиеся заклинания (21)

| файл | имя | что нарисовать |
|---|---|---|
| `icon-magnit` | МАГНИТ | подкова с двумя полюсами |
| `icon-tuman` | ТУМАН | три горизонтальные размытые полосы |
| `icon-geyzer` | ГЕЙЗЕР | столб из трещины в земле |
| `icon-peregrev` | ПЕРЕГРЕВ | термометр со шкалой на пределе |
| `icon-pepel` | ПЕПЕЛ | четыре падающих хлопья |
| `icon-smerch` | СМЕРЧ | воронка, широкая сверху |
| `icon-vulkan` | ВУЛКАН | гора с выбросом из вершины |
| `icon-metel` | МЕТЕЛЬ | снежинка с шестью лучами, наклонена |
| `icon-groza` | ГРОЗА | туча, из неё разряд |
| `icon-zybun` | ЗЫБУН | три круга на поверхности, один в другом |
| `icon-burya` | БУРЯ | две закрученные линии ветра |
| `icon-borozda` | БОРОЗДА | след из пламени, тянется за точкой |
| `icon-ledostav` | ЛЕДОСТАВ | волна, схваченная льдом |
| `icon-peskostruy` | ПЕСКОСТРУЙ | сопло со струёй под давлением |
| `icon-hvatka` | ХВАТКА | четыре стрелки внутрь, к центру |
| `icon-otboy` | ОТБОЙ | четыре стрелки наружу |
| `icon-razryadnik` | РАЗРЯДНИК | три точки, соединённые разрядом |
| `icon-ruslo` | РУСЛО | извилистая дорожка воды |
| `icon-pal` | ПАЛ | сплошная стена огня |
| `icon-shtorm` | ШТОРМ | волна с гребнем и ветром над ней |
| `icon-zavesa` | ЗАВЕСА | облако-полог, свисает вниз |

## Пять стихий для книги (5)

В книге пять стихий стоят первой строкой рядом с остальными и пока не имеют
картинки: те, что в игре, — белые силуэты, которые игра перекрашивает под
цвет стихии, и рядом с рисованными они смотрятся чужими. Нужны отдельные,
рисованные, **под новыми именами** — старые не трогать.

| файл | имя | что нарисовать |
|---|---|---|
| `icon-ogon` | ОГОНЬ | язык пламени |
| `icon-voda` | ВОДА | капля с бликом |
| `icon-veter` | ВЕТЕР | три линии потока |
| `icon-zemlya` | ЗЕМЛЯ | камень с гранями |
| `icon-molniya` | МОЛНИЯ | разряд-стрела |

---

# 2. Предметы, 31 файл, 192×192

Прошлый заход прислал один семиугольник на все — его выбросить. У каждой
вещи **свой контур**, и он узнаётся раньше цвета.

| предмет | контур | приметы |
|---|---|---|
| `prop-barrel` | цилиндр стоймя, верх овалом | обручи, крышка, потёки ржавчины |
| `prop-canister` | плоская коробка с ручкой | носик, наклейка |
| `prop-tank` | лежачий цилиндр на опорах | вентиль, сварной шов |
| `prop-pipe` | труба поперёк кадра | фланцы, подпорки |
| `prop-crystal` | острый многогранник | грани, свечение изнутри |
| `prop-hay` | рулон соломы | перевязь, торчащие стебли |
| `prop-boulder` | неровный валун | скол, трещина, мох |
| `prop-generator` | ящик с решёткой | провода, рубильник |
| `prop-fridge` | высокий прямоугольник | дверца со швом, ручка, магнитики |
| `prop-vending` | высокий шкаф со стеклом | ряды банок, кнопки |
| `prop-junk` | бесформенная куча | доски, покрышка, тряпьё |
| `prop-block` | бетонный параллелепипед | арматура из скола |
| `prop-neon` | вывеска на кронштейне | трубки, крепление |
| `prop-wreck` | остов машины боком | колёса, разбитая фара |
| `prop-cart` | тележка на колёсах | решётка, ручка |
| `prop-bench` | скамья в профиль | доски, чугунные ножки |

К каждому — файл `-broken`, **кроме скамейки**: она не ломается. Итого 31.

Разбитый вариант — **та же вещь, но развороченная**, и она обязана
оставаться узнаваемой: у бочки лопнули обручи и разошлись доски, у
холодильника вырвана дверца и висит на петле, у автомата выбито стекло и
рассыпались банки, у валуна осталась половина и щебень.

**Зачем это игре.** По виду предмета игрок решает, чем в него бить: бочка
обещает воду, канистра — огонь, кристалл — разряд, стог — пожар. Сейчас
игра рисует бочку сама, тёмным ободом со светящейся водой внутри, и это
читается лучше присланного семиугольника. Пока предмет не узнаётся по
контуру, ставить его в игру нельзя — будет хуже, чем есть.

---

# 3. Маги, 33 листа

## Раскладка

**Строки — восемь сторон света** в порядке `вправо`, `вправо-вниз`, `вниз`,
`влево-вниз`, `влево`, `влево-вверх`, `вверх (спина)`, `вправо-вверх`.
**Столбцы — кадры.** Кадр 192×192.

Строка «вверх» — фигура спиной, лица не видно вовсе. Восемь строк обязаны
отличаться: игрок по фигуре понимает, куда она смотрит, и от этого зависит,
зайдёт он со спины или нет. В прошлый раз это было сделано верно — сохранить.

| роль | цвет | кто это | листы |
|---|---|---|---|
| `player` | `#c4d1d8` + `#24dce6` | игрок, ирокез, посох с шаром | idle 4, walk 6, cast 4 |
| `punk` | `#a51e31` | панк с трубой, сутулый | idle 4, walk 6, swing 4 |
| `sparker` | `#318ed8` | искровик, дуги между ладонями | idle 4, walk 6, cast 4 |
| `warden` | `#ecd24b` | страж, кокон-кольцо | idle 4, walk 6 |
| `brute` | `#8a4b2b` | бугай в резине и покрышках | idle 4, walk 6, slam 4 |
| `drone` | `#596474` + `#24dce6` | висит в воздухе, винты, глазок | idle 4, walk 6, zap 4 |
| `runner` | `#f23891` | тощий бегун, вечно в рывке | idle 4, walk 6, dash 4 |
| `domer` | `#bc70ff` | купольщик, руки вверх | idle 4, walk 6, shield 4 |
| `boss` | `#ef6a2b` | БАТЯ, вдвое выше, борода, две трубы | idle 4, walk 6, slam 4 |

Плюс `mage-corpse` (1 кадр, 8 строк) и два запасных облика игрока
`mage-player2-*` и `mage-player3-*` (idle 4, walk 6, cast 4).

Размеры: `idle` и лист действия — 768×1536, `walk` — 1152×1536,
`mage-corpse` — 192×1536. У БАТИ кадр 288×288: `idle` и `slam` — 1152×2304,
`walk` — 1728×2304.

## Чего не хватало дважды

Фигура состоит из трапеции, кружка-головы и двух палочек. Роли различаются,
восемь сторон работают — это сохранить. Добавить:

* **руки и кисти** — видно, что маг держит посох, а панк замахивается трубой;
* **ноги в шаге** — в листах `walk` фигура не переставляет ног;
* **одежду** — полы куртки, ремни, заклёпки, изолента;
* **причёску силуэтом** — ирокез, хвост, лысина с бородой;
* **лицо там, где оно видно** — как в портретах, которые получились.

Портреты доказали, что лица рисуются. Те же головы нужны на фигурах.

---

# 4. Вспышки, 5 листов

Игра уже показывает искры, дым и кольца; этих трёх мало на пять стихий.
Лист — **шесть кадров в ряд**, кадр 128×128, файл **768×128**. Фон
прозрачный, кадры гаснут к последнему.

| файл | что это |
|---|---|
| `fx-burst` | взрыв: вспышка, ошмётки, дым |
| `fx-steam` | клуб пара снизу вверх |
| `fx-ice` | лёд схватывается и лопается |
| `fx-sand` | облако песка с крошкой |
| `fx-arc` | разряд между двумя точками, дёргается |

---

# 5. Парк, 7 файлов, 192×192

Обстановка, которую нельзя сломать, — она просто стоит и делает место
живым. Прозрачный фон.

`deco-swing` (качели), `deco-carousel` (карусель), `deco-kiosk` (ларёк с
заколоченным окном), `deco-pylon` (опора ЛЭП с гирляндой),
`deco-graffiti-1`, `deco-graffiti-2`, `deco-graffiti-3` (рисунки на
асфальте, вид сверху: тег, череп, звезда).

---

# 6. Обложка, 1 файл

`cover.png`, 1200×630, **без единой надписи** — ни названия, ни
«pre-order», ни «coming soon». Игра открывается по ссылке в браузере,
предзаказывать нечего.

---

# Общее для всех файлов

**Стиль.** Техномаги-раздолбаи, панки: ирокезы, куртки с заклёпками,
изолента, самокрутки. Вокруг постапокалипсис, но всем весело — граффити,
неон, гирлянды на бетоне. Плотная детальная графика с крупной, почти
пиксельной фактурой и мягким свечением поверх. Не мультяшно и не глянцево.
**Ориентир — те 16 значков, что уже приняты.**

**Палитра — та, что стоит в игре.** Другая будет видна сразу.

```
ink #10131b    dark #202633   steel #596474  light #c4d1d8
cyan #24dce6   pink #f23891   orange #ef6a2b blue #318ed8
green #4f9d59  mud #684a31    ice #82d0e8    red #a51e31
yellow #ecd24b white #ffffff  rust #8a4b2b   skin #c69a76
violet #bc70ff
```

**Жёсткое.**

* PNG с прозрачностью, без фона и без рамок вокруг кадра.
* **Тень под фигурой не рисовать** — у восьми сторон тень одна, её кладёт
  игра. Свет и тень на самой фигуре — можно и нужно.
* Ни одной буквы ни на чём.
* Размеры точные до пикселя: игра режет лист по сетке, и сдвиг на пиксель
  поедет по всем кадрам.
* Имена файлов — ровно как в таблицах, латиницей.

**Порядок сдачи.** Партиями по 8–16 файлов, начиная со значков: они
проверяются быстрее всего, и по ним видно, держится ли планка. Дальше
предметы, вспышки, парк, обложка, маги.

## Итого

| раздел | файлов |
|---|---|
| значки заклинаний | 21 |
| значки стихий для книги | 5 |
| предметы | 31 |
| маги | 33 |
| вспышки | 5 |
| парк | 7 |
| обложка | 1 |
| **всего** | **103** |

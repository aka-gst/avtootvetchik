# ТЕХНОМАГИЯ — что доделать и что переделать

Один документ вместо всех прежних: он самодостаточный, его пересылают
целиком. Игра — браузерный экшен про техномагов-раздолбаев в
постапокалиптическом парке. Стихии смешиваются, вещество остаётся лежать на
полу, разряд идёт по разлитой воде.

Пришло 142 файла. **Скелет собран безупречно, рисунка в нём нет.**

---

# ЧАСТЬ 1. Что принято и переделке не подлежит

Это проверено программно, менять не нужно:

* **Все 142 размера точны до пикселя.** 33 листа магов, включая БАТЮ кадром
  288×288 и листом 1728×2304. Ни одного расхождения.
* **Восемь сторон света работают.** Посох и голова поворачиваются по кругу,
  строка «вверх» — спиной, лица не видно.
* **Цвета по ролям расставлены верно**, и роли на тёмно-зелёном фоне
  различаются с одного взгляда.
* **Стены: во всех шести листах все 16 клеток разные.** Автосборка
  стыкуется.

Имена файлов, размеры и раскладку листов сохранить как есть.

---

# ЧАСТЬ 2. Две проверки перед отправкой

Первую прошлая поставка прошла, вторую провалила. Обе делаются за минуту.

**Проверка на размер.** Уменьшить лист до 40 пикселей на кадр, положить
фигуры на тёмно-зелёный фон `#16342d`. Различаются ли они? Видно ли, куда
смотрят? На экране фигура занимает 26–40 пикселей — всё, что тоньше,
пропадает.

**Проверка на контур.** Сложить силуэты файлов одной группы и посчитать
различные. **Совпал контур — это один рисунок в двух цветах.** Из 37
значков должно выйти 37 разных контуров, из 24 предметов — 24.

Прошлая поставка на второй проверке дала: значки 37 файлов → **5 контуров**,
предметы 24 файла → **2 контура**.

---

# ЧАСТЬ 3. ПЕРЕДЕЛАТЬ — по порядку пользы для игры

## 1. Значки, 37 файлов, 64×64

Тринадцать значков — одна фигура в разных цветах, ещё десять — другая. На 37
заклинаний нарисовано пять форм: три полоски, ромб, звёздочка, кольцо, дуга.

Эти значки стоят в книге заклинаний сеткой, рядом друг с другом. Книга —
главный экран игры после поля, и сейчас она нечитаема: ПАР, ЛАВА и ПЛАЗМА —
три одинаковые полоски. **Отличать 37 наименований по цвету нельзя.**

Значок — это знак: одна крупная форма, два-три цвета, читается в 24 пикселя.
Фон прозрачный.

### Формы — как летит заклинание (7)

| файл | имя | форма |
|---|---|---|
| `icon-plevok` | ПЛЕВОК | одна капля вперёд |
| `icon-sgustok` | СГУСТОК | тяжёлый ком с хвостом |
| `icon-vydoh` | ВЫДОХ | конус из точки |
| `icon-zalp` | ЗАЛП | три снаряда веером |
| `icon-luch` | ЛУЧ | прямая линия насквозь |
| `icon-proboy` | ПРОБОЙ | линия, пробившая стену |
| `icon-vspyshka` | ВСПЫШКА | кольцо во все стороны |

### Вещества из смеси стихий (20)

| файл | имя | форма |
|---|---|---|
| `icon-par` | ПАР | струя вверх из-под крышки |
| `icon-zhar` | ЖАР | волны марева |
| `icon-lava` | ЛАВА | стекающая капля с коркой |
| `icon-plazma` | ПЛАЗМА | шар в кольце |
| `icon-stuzha` | СТУЖА | кристалл льда |
| `icon-gryaz` | ГРЯЗЬ | комок с брызгами |
| `icon-razryad` | РАЗРЯД | ломаная стрела |
| `icon-pesok` | ПЕСОК | струя песчинок |
| `icon-grom` | ГРОМ | круги звука |
| `icon-magnit` | МАГНИТ | подкова |
| `icon-tuman` | ТУМАН | три размытые полосы внахлёст |
| `icon-geyzer` | ГЕЙЗЕР | столб из земли |
| `icon-peregrev` | ПЕРЕГРЕВ | термометр на пределе |
| `icon-pepel` | ПЕПЕЛ | падающие хлопья |
| `icon-smerch` | СМЕРЧ | воронка |
| `icon-vulkan` | ВУЛКАН | гора с выбросом |
| `icon-metel` | МЕТЕЛЬ | снежинка под наклоном |
| `icon-groza` | ГРОЗА | туча с разрядом |
| `icon-zybun` | ЗЫБУН | круги на поверхности |
| `icon-burya` | БУРЯ | закрученные линии ветра |

### Именные заклинания (10) — их находят, и это событие

| файл | имя | форма |
|---|---|---|
| `icon-borozda` | БОРОЗДА | огненный след за собой |
| `icon-ledostav` | ЛЕДОСТАВ | лёд, схвативший воду |
| `icon-peskostruy` | ПЕСКОСТРУЙ | сопло со струёй |
| `icon-hvatka` | ХВАТКА | стрелки внутрь, к центру |
| `icon-otboy` | ОТБОЙ | стрелки наружу |
| `icon-razryadnik` | РАЗРЯДНИК | разряд, идущий по цепи из трёх точек |
| `icon-ruslo` | РУСЛО | дорожка воды |
| `icon-pal` | ПАЛ | стена огня |
| `icon-shtorm` | ШТОРМ | волна с ветром |
| `icon-zavesa` | ЗАВЕСА | облако-полог |

Пять значков стихий (`icon-fire`, `icon-water`, `icon-wind`, `icon-earth`,
`icon-bolt`) уже стоят в игре и переделке не подлежат.

## 2. Предметы, 31 файл, 192×192

Все восемь целых предметов пришли одним пятиугольником, все шестнадцать
разбитых — одной щербатой кляксой. В игре по виду предмета игрок решает, чем
в него бить: бочка обещает воду, канистра — огонь, кристалл — разряд, стог —
пожар. Одинаковые пятиугольники этой работы не делают.

Разбитый вариант — **та же вещь, но развороченная**: у бочки лопнули обручи
и разошлись доски, у холодильника вырвана дверца, у автомата выбито стекло.

| предмет | приметы | ломается |
|---|---|---|
| `prop-barrel` | обручи, крышка, потёки ржавчины | водой изнутри |
| `prop-canister` | ручка, носик, наклейка | вспыхивает |
| `prop-tank` | цистерна на опорах, вентиль | много воды |
| `prop-crystal` | грани, свечение изнутри | только разрядом |
| `prop-hay` | солома, перевязь | горит целиком |
| `prop-boulder` | скол, трещина, мох | дробится |
| `prop-generator` | кожух, провода, решётка | бьёт током в ответ |
| `prop-fridge` | дверца, ручка, магнитики | заваливается |
| `prop-vending` | стекло, ряды банок, кнопки | стекло вылетает |
| `prop-junk` | доски, покрышка, тряпьё | разлетается |
| `prop-block` | бетонный блок, арматура | крошится |
| `prop-neon` | вывеска на кронштейне, трубки | трубки бьются |
| `prop-wreck` | остов машины, разбитая фара | горит и оседает |
| `prop-pipe` | труба на подпорках, фланец | лопается |
| `prop-cart` | колёса, решётка, ручка | складывается |
| `prop-bench` | доски, чугунные ножки | **не ломается**, `-broken` не нужен |

**Целые предметы в прошлый раз не пришли вовсе — и это моя ошибка в
задании:** я написал «есть», хотя в игре их нет, они выпали при откате.
Нужны и целые, и разбитые: 15 пар плюс скамейка = 31 файл.

## 3. Портреты, 9 файлов, 320×320

Пришли те же фигурки в полный рост, ужатые в угол кадра. Портрет — это
**голова и плечи во весь кадр**: лицо, причёска, ирокез, серьги, шрам,
выражение. Сейчас лица нет ни у одного.

`face-player`, `face-punk`, `face-sparker`, `face-warden`, `face-brute`,
`face-drone`, `face-runner`, `face-domer`, `face-boss`.

У панка, бегуна и искровика контуры совпали между собой — они должны быть
тремя разными людьми.

## 4. Маги, 33 листа

На полном размере 192×192 фигура состоит из трапеции, кружка-головы и двух
палочек. Роли различаются — это уже хорошо и это сохранить. Но игра выглядит
незаконченной не потому, что фигуры неразличимы, а потому, что они не
нарисованы.

Нужно добавить:

* **руки и кисти** — видно, что маг держит посох, а панк замахивается трубой;
* **ноги в шаге** — в листах `walk` фигура сейчас не переставляет ног;
* **одежду** — полы куртки, ремни, заклёпки, изолента;
* **причёску силуэтом** — у игрока ирокез, у панка хвост, у бугая лысина и
  борода;
* **лицо там, где оно видно** — глаза и рот двумя-тремя точками довольно.

Раскладка листа прежняя: **строки — восемь сторон света** в порядке `вправо`,
`вправо-вниз`, `вниз`, `влево-вниз`, `влево`, `влево-вверх`, `вверх (спина)`,
`вправо-вверх`; **столбцы — кадры**; кадр 192×192.

| роль | цвет | кто это | листы |
|---|---|---|---|
| `player` | `#c4d1d8` + `#24dce6` | игрок, ирокез, посох с шаром | idle 4, walk 6, cast 4 |
| `punk` | `#a51e31` | панк с трубой, сутулый | idle 4, walk 6, swing 4 |
| `sparker` | `#318ed8` | искровик, дуги между ладонями | idle 4, walk 6, cast 4 |
| `warden` | `#ecd24b` | страж, кокон-кольцо | idle 4, walk 6 |
| `brute` | `#8a4b2b` | бугай в резине и покрышках | idle 4, walk 6, slam 4 |
| `drone` | `#596474` + `#24dce6` | висит в воздухе, винты, глазок | idle 4, walk 6, zap 4 |
| `runner` | `#f23891` | тощий бегун, вечно в рывке | idle 4, walk 6, dash 4 |
| `domer` | `#bc70ff` | купольщик, руки вверх | idle 4, walk 6, shield 4 |
| `boss` | `#ef6a2b` | БАТЯ, вдвое выше, борода, две трубы | idle 4, walk 6, slam 4 |

Плюс `mage-corpse` (1 кадр, 8 строк) и два запасных облика игрока
`mage-player2-*`, `mage-player3-*`.

Размеры: обычный лист `idle`/действие — 768×1536, `walk` — 1152×1536,
`mage-corpse` — 192×1536. У БАТИ кадр 288×288: `idle`/`slam` — 1152×2304,
`walk` — 1728×2304.

---

# ЧАСТЬ 4. Что можно не трогать

Эти файлы пришли и вопросов не вызывают: 9 плиток пола, 6 листов стен, 12
посохов и вещиц, 11 файлов интерфейса. Если дойдут руки — вещицам и посохам
тоже не помешают разные контуры (сейчас из 12 файлов 5 форм), но это
последняя очередь.

`cover.png` переделать в одном: **без единой надписи** — ни названия, ни
«pre-order», ни «coming soon». Игра открывается по ссылке в браузере,
предзаказывать нечего.

---

# Общее для всех файлов

**Стиль.** Техномаги-раздолбаи, панки: ирокезы, куртки с заклёпками,
изолента, самокрутки. Вокруг постапокалипсис, но всем весело — граффити,
неон, гирлянды на бетоне. Плотная детальная графика с крупной, почти
пиксельной фактурой и мягким свечением поверх. Не мультяшно и не глянцево.

**Палитра — та, что стоит в игре.** Другая будет видна сразу.

```
ink #10131b    dark #202633   steel #596474  light #c4d1d8
cyan #24dce6   pink #f23891   orange #ef6a2b blue #318ed8
green #4f9d59  mud #684a31    ice #82d0e8    red #a51e31
yellow #ecd24b white #ffffff  rust #8a4b2b   skin #c69a76
violet #bc70ff
```

**Жёсткое.**

* PNG с прозрачностью, без фона и без рамок вокруг кадра.
* **Тень под фигурой не рисовать** — у восьми сторон тень одна, её кладёт
  игра. Свет и тень на самой фигуре — можно и нужно.
* Ни одной буквы ни на чём, кроме файлов ранга `ui-rank-*`.
* Размеры точные до пикселя: игра режет лист по сетке, и сдвиг на пиксель
  поедет по всем кадрам.
* Имена файлов — ровно как в таблицах, латиницей.

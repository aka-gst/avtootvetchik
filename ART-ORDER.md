# ТЕХНОМАГИЯ — заказ на графику

Документ самодостаточный: его пересылают целиком. Игра — браузерный экшен
про техномагов-раздолбаев в постапокалиптическом парке. Стихии смешиваются,
вещество остаётся лежать на полу, разряд идёт по разлитой воде.

## Главное правило, из-за которого два прошлых захода не подошли

**На экране фигура занимает 26–40 пикселей.** Игра уменьшает кадр 192×192
примерно в пять раз. Прошлые маги были серым кружком и серым кружком с
полосой; в игре и то и другое превращалось в бурую точку.

Отсюда проверка, и её нужно делать **до отправки**:

1. Уменьшить лист до 40 пикселей на кадр.
2. Положить фигуры рядом на тёмно-зелёный фон `#16342d`.
3. Различаются ли они с одного взгляда? Видно ли, куда фигура смотрит?

Если нет — лист не готов, сколько бы деталей ни было в оригинале. Читается
контур: плечи, подол, ирокез, вынесенный посох. Круглая фигура не читается
ничем.

## Порядок работы: волнами, а не всё сразу

Прошлый раз пришло восемьдесят три файла, и все мимо. Поэтому сначала
**волна 1 — семь файлов**. По ним видно, сходится ли стиль. Дальше остальное.

---

# ВОЛНА 1 — проба, 7 файлов

| файл | размер | что это |
|---|---|---|
| `mage-player-idle.png` | 768×1536 | игрок, покой, 8 сторон × 4 кадра |
| `mage-punk-idle.png` | 768×1536 | панк, покой |
| `prop-barrel.png` | 192×192 | бочка |
| `prop-barrel-broken.png` | 192×192 | она же разбитая |
| `icon-par.png` | 64×64 | значок вещества ПАР |
| `icon-lava.png` | 64×64 | значок вещества ЛАВА |
| `icon-luch.png` | 64×64 | значок формы ЛУЧ |

Дальше — по волнам ниже.

---

# ВОЛНА 2 — маги

Раскладка листа: **строки — восемь сторон света** в порядке `вправо`,
`вправо-вниз`, `вниз`, `влево-вниз`, `влево`, `влево-вверх`, `вверх (спина)`,
`вправо-вверх`. **Столбцы — кадры.** Кадр `192×192`.

Строка «вверх» — фигура **спиной**, лица не видно вовсе. Восемь строк
обязаны отличаться: игрок по фигуре понимает, куда она смотрит, и от этого
зависит, зайдёт он со спины или нет.

## Кто уже в игре

| роль | цвет | силуэт | листы |
|---|---|---|---|
| `player` — игрок | светлый холодный `#c4d1d8` + `#24dce6` | самый светлый на экране, высокий ирокез, посох с шаром | `idle` 4, `walk` 6, `cast` 4 |
| `punk` — панк с трубой | красный `#a51e31`, кожа `#c69a76` | шире всех, труба наперевес, сутулый | `idle` 4, `walk` 6, `swing` 4 |
| `sparker` — искровик | синий `#318ed8` | тощий, руки в стороны, дуги между ладонями | `idle` 4, `walk` 6, `cast` 4 |
| `warden` — страж | жёлтый `#ecd24b` | приземистый, вокруг кокон-кольцо | `idle` 4, `walk` 6 |

Плюс `mage-corpse.png` — 1 кадр, 8 строк, 192×1536.

## Кого добавляем

Каждый придуман под механику: у игры пять стихий, и враг интересен тем,
чем его нельзя убить.

| роль | цвет | кто это | чем берётся |
|---|---|---|---|
| `brute` — бугай | ржавый `#8a4b2b` | огромный, в резиновых накладках и покрышках | ток не берёт вовсе, только жар и лава |
| `drone` — дрон | стальной `#596474` + `#24dce6` | висит в воздухе, винты, глазок | лужа под ним бесполезна, только прямой удар |
| `runner` — бегун | розовый `#f23891` | тощий, полусогнут, вечно в рывке | гибнет от чего угодно, если догонишь |
| `domer` — купольщик | фиолетовый `#bc70ff` | сидит под куполом, руки вверх | купол пробивают только ЛУЧ и ПРОБОЙ |
| `boss` — БАТЯ | оранжевый `#ef6a2b` | вдвое выше прочих, борода, две трубы | всё сразу и по очереди |

Листы для каждого: `idle` 4 кадра, `walk` 6, действие 4.
Имена: `mage-brute-idle.png`, `mage-brute-walk.png`, `mage-brute-slam.png`,
`mage-drone-idle/walk/zap`, `mage-runner-idle/walk/dash`,
`mage-domer-idle/walk/shield`, `mage-boss-idle/walk/slam`.

**БАТЯ рисуется кадром 288×288**, лист `walk` — 1728×2304. Он крупнее всех.

## Игроку — три облика

Один и тот же силуэт, разная раскраска и причёска: `mage-player-idle.png`
уже есть, добавить `mage-player2-*` и `mage-player3-*` (idle/walk/cast).

---

# ВОЛНА 3 — значки, 49 штук по 64×64

Их видно в книге заклинаний и в очереди набора. Значок — **не иллюстрация,
а знак**: одна форма, два-три цвета, читается в 24 пикселя. Фон прозрачный.

## Стихии (5) — уже есть, переделывать не нужно

`icon-fire`, `icon-water`, `icon-wind`, `icon-earth`, `icon-bolt`.

## Формы — как летит заклинание (7)

| файл | имя | что показать |
|---|---|---|
| `icon-plevok.png` | ПЛЕВОК | одна капля вперёд |
| `icon-sgustok.png` | СГУСТОК | тяжёлый ком |
| `icon-vydoh.png` | ВЫДОХ | конус |
| `icon-zalp.png` | ЗАЛП | три штуки веером |
| `icon-luch.png` | ЛУЧ | прямая линия насквозь |
| `icon-proboy.png` | ПРОБОЙ | линия, пробившая стену |
| `icon-vspyshka.png` | ВСПЫШКА | кольцо во все стороны |

## Вещества из смеси стихий (20)

`icon-par` ПАР · `icon-zhar` ЖАР · `icon-lava` ЛАВА · `icon-plazma` ПЛАЗМА ·
`icon-stuzha` СТУЖА · `icon-gryaz` ГРЯЗЬ · `icon-razryad` РАЗРЯД ·
`icon-pesok` ПЕСОК · `icon-grom` ГРОМ · `icon-magnit` МАГНИТ ·
`icon-tuman` ТУМАН · `icon-geyzer` ГЕЙЗЕР · `icon-peregrev` ПЕРЕГРЕВ ·
`icon-pepel` ПЕПЕЛ · `icon-smerch` СМЕРЧ · `icon-vulkan` ВУЛКАН ·
`icon-metel` МЕТЕЛЬ · `icon-groza` ГРОЗА · `icon-zybun` ЗЫБУН ·
`icon-burya` БУРЯ

## Именные заклинания (10) — их находят, и это событие

`icon-borozda` БОРОЗДА (огненный след за собой) ·
`icon-ledostav` ЛЕДОСТАВ (лёд по воде) ·
`icon-peskostruy` ПЕСКОСТРУЙ · `icon-hvatka` ХВАТКА (притягивает) ·
`icon-otboy` ОТБОЙ (отбрасывает) · `icon-razryadnik` РАЗРЯДНИК (ток по цепи) ·
`icon-ruslo` РУСЛО (вода дорожкой) · `icon-pal` ПАЛ ·
`icon-shtorm` ШТОРМ · `icon-zavesa` ЗАВЕСА (облако)

## Посохи и артефакты (12), 96×96

Инвентарь на будущее: игрок носит один посох и пару вещиц.
`item-staff-1..4` — посохи разного склада (труба с катушкой, костыль с
кристаллом, антенна, арматура с изолентой).
`item-charm-1..8` — вещицы: аккумулятор, катушка, склянка, шестерня,
предохранитель, кассета, зажигалка, магнит.

---

# ВОЛНА 4 — уровни

Сейчас в игре один район: асфальт, трава, краска, бетонная стена, сетка.
Нужны ещё три, чтобы этажи отличались на вид.

**Автосборка: лист 4×4, кадр 192×192, файл 768×768.** Место клетки в листе
считается по битам соседей: верх=1, право=2, низ=4, лево=8, счёт с нуля.
Игра читает **место клетки, а не цифру на ней** — рисовать номера не нужно.

| район | пол (3 файла) | стены (2 файла) |
|---|---|---|
| депо | `floor-depot-1..3` | `wall-container`, `wall-rail` |
| торговый центр | `floor-mall-1..3` | `wall-glass`, `wall-shelf` |
| крыши | `floor-roof-1..3` | `wall-parapet`, `wall-vent` |

Пол — одиночные плитки 192×192, стены — листы автосборки 768×768.

## Предметы, которые ломают

Есть: `prop-barrel`, `prop-canister`, `prop-junk`, `prop-block`, `prop-neon`,
`prop-fridge`, `prop-wreck`, `prop-bench`. К каждому нужен `-broken`.

Добавить, по 192×192, каждый с `-broken`: `prop-hay` (стог, горит),
`prop-crystal` (кристалл, бьётся только разрядом), `prop-boulder` (валун),
`prop-tank` (цистерна, много воды), `prop-generator` (генератор, бьёт током),
`prop-vending` (автомат), `prop-pipe` (труба), `prop-cart` (тележка).

По виду предмета игрок должен догадываться, чем его брать: бочка обещает
воду, канистра — огонь, кристалл — разряд.

---

# ВОЛНА 5 — интерфейс

| файл | размер | что это |
|---|---|---|
| `ui-bar-health.png` | 256×32 | полоса живучести, рамка отдельно от заливки |
| `ui-bar-mana.png` | 256×32 | полоса силы |
| `ui-bar-xp.png` | 256×16 | полоса опыта |
| `ui-frame-slot.png` | 96×96 | ячейка инвентаря, пустая |
| `ui-frame-portrait.png` | 160×160 | рамка портрета |
| `ui-minimap.png` | 256×256 | рамка карты, середина прозрачная |
| `ui-rank-S/A/B/C/D.png` | 128×128 | буквы ранга по итогам этажа |

## Портреты для разговоров, 320×320

Поясные, лицом к зрителю, фон прозрачный: `face-player`, `face-punk`,
`face-sparker`, `face-warden`, `face-brute`, `face-drone`, `face-runner`,
`face-domer`, `face-boss`.

---

# Общее для всех файлов

**Стиль.** Техномаги-раздолбаи, панки: ирокезы, куртки с заклёпками,
изолента, самокрутки. Вокруг постапокалипсис, но всем весело — граффити,
неон, гирлянды на бетоне. Плотная детальная графика с крупной, почти
пиксельной фактурой и мягким свечением поверх. Не мультяшно и не глянцево.

**Палитра — та, что уже стоит в игре.** Другая будет видна сразу.

```
ink #10131b    dark #202633   steel #596474  light #c4d1d8
cyan #24dce6   pink #f23891   orange #ef6a2b blue #318ed8
green #4f9d59  mud #684a31    ice #82d0e8    red #a51e31
yellow #ecd24b white #ffffff  rust #8a4b2b   skin #c69a76
violet #bc70ff
```

**Жёсткое.**

* PNG с прозрачностью, без фона и без рамок вокруг кадра.
* **Тень под фигурой не рисовать** — у восьми сторон тень одна, её кладёт
  игра. Свет и тень **на самой фигуре** — можно и нужно.
* Ни одной буквы ни на чём, кроме файлов ранга.
* Размеры точные до пикселя: игра режет лист по сетке и сдвиг на пиксель
  поедет по всем кадрам.
* Имена файлов — ровно как в таблицах, латиницей.

**И отдельно:** `cover.png`, 1200×630, **без единой надписи** — ни названия,
ни «pre-order», ни «coming soon». Игра открывается по ссылке в браузере,
предзаказывать нечего.
